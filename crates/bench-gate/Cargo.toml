[package]
name = "bench-gate"
description = "CI gate for criterion benchmark results — floor and regression checks"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
# Internal CI tooling — never published to crates.io.
publish = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
